// Mirrors config/community-signals.ts's COMMUNITY_STRATEGY_ID in the main
// app (separate repo, no shared code) — the fixed strategy_id every
// community signal is generated under.
export const COMMUNITY_STRATEGY_ID = "system_swing_starter";
